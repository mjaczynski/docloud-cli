

var docloud = require('../index.js');
var fs = require('fs');
var client = docloud({
	  url : process.env.URL,
	  clientId : process.env.KEY
	})
	
console.log('Using DOcloud URL: ',process.env.URL)

it ('command-line', function(done){
	

});
